version: "3.8"

networks:
  internal:
    driver: bridge
    enable_ipv6: true

volumes:
  redis-data:
  rabbitmq-data:
  waha-media:

services:
  api:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    environment:
      NODE_ENV: production
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - internal
    ports:
      - "5000:5000"
    volumes:
      - waha-media:/waha-media:ro  # Mount WAHA media directory as read-only
    restart: unless-stopped

  worker-inbound:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    command: ["node", "dist/worker.js", "inbound"]
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    networks:
      - internal
    volumes:
      - waha-media:/waha-media:ro  # Mount WAHA media directory as read-only

  worker-outbound:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    command: ["node", "dist/worker.js", "outbound"]
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    networks:
      - internal
    volumes:
      - waha-media:/waha-media:ro  # Mount WAHA media directory as read-only

  worker-media:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    command: ["node", "dist/worker.js", "inbound-media"]
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    networks:
      - internal
    volumes:
      - waha-media:/waha-media:ro  # Mount WAHA media directory as read-only

  worker-campaigns:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    command: ["node", "dist/worker.campaigns.js"]
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    networks:
      - internal

  worker-flows:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - ./backend/.env.production
    command: ["node", "dist/worker.flows.js"]
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    networks:
      - internal

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api-back.7sion.com}
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:-https://ghbiigjdvzeoouxaviyz.supabase.co}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:-eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdoYmlpZ2pkdnplb291eGF2aXl6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTU1Njk3MjQsImV4cCI6MjA3MTE0NTcyNH0.Ze8yBaQB7ZD-k4s6hj63st2xjjah4l9GawyaMjsEi8o}
        VITE_APP_BASE_PATH: ${VITE_APP_BASE_PATH:-/}
        VITE_APP_NAME: ${VITE_APP_NAME:-LiveChat}
        VITE_APP_VERSION: ${VITE_APP_VERSION:-1.0.0}
        VITE_PUBLIC_URL: ${VITE_FRONTEND_PUBLIC_URL:-https://app.7sion.com}
    depends_on:
      - api
    ports:
      - "3002:80"
    networks:
      - internal
    restart: unless-stopped

  cadastro:
    build:
      context: .
      dockerfile: ./cadastro/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api-back.7sion.com}
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:-https://ghbiigjdvzeoouxaviyz.supabase.co}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:-eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdoYmlpZ2pkdnplb291eGF2aXl6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTU1Njk3MjQsImV4cCI6MjA3MTE0NTcyNH0.Ze8yBaQB7ZD-k4s6hj63st2xjjah4l9GawyaMjsEi8o}
        VITE_APP_BASE_PATH: ${VITE_APP_BASE_PATH:-/}
        VITE_PUBLIC_URL: ${VITE_CADASTRO_PUBLIC_URL:-https://account.7sion.com}
    depends_on:
      - api
    ports:
      - "3003:80"
    networks:
      - internal
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - internal
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-app}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-app}
    ports:
      - "15672:15672"
    networks:
      - internal
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 10
